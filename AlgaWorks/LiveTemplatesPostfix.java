import java.security.spec.RSAOtherPrimeInfo;

public class LiveTemplatesPostfix {

    public static void main(String[] args) {
        int total = 10;
        boolean pendente = true;

        // Live template: souf
        System.out.printf("");

        // Postfix: total.fori
        for (int i = 0; i < total; i++) {

        }

        // Postfix: total.forr
        for (int i = total; i > 0; i--) {
            
        }

        // Postfix: total.switch
        switch (total) {

        }

        // Existem esses e vários outros atalhos uteis para a produtividade
        // A visualização de todos está disponivel em: settings/editor/live templates
    }
}